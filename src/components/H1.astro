---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"h1"> {
  text: string;
}

const { text, ...rest } = Astro.props;
---

<h1 {...rest} class="text-6xl text-teal-900 font-bold mb-5">{text}</h1>
